openapi: 3.0.3
info:
  title: St. Peter API
  description: API for autorisation av tilganger til brukere.
  version: 1.0.0
  contact:
    name: "#team-dagpenger"
servers:
  - url: https://dp-st-peter.intern.dev.nav.no
    description: dev
#  - url: https://dp-st-peter.intern.nav.no
#    description: prod
paths:
  /api/person:
    post:
      summary: Sjekk saksbehandlers tilgang til bruker
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/IdentForesporsel"
      tags:
        - Brukertilgang
      security:
        - azureAd: [ ]
      responses:
        "204":
          description: Saksbehandler har tilgang til bruker
        "403":
          description: Saksbehandler har ikke tilgang til bruker
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HttpProblem"
        "404":
          description: NavIdent ikke funnet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HttpProblem"
        default:
          description: Feil ved henting av brukertilgang
          content:
            application/problem+json:
              schema:
                $ref: "#/components/schemas/HttpProblem"

components:
  schemas:

    IdentForesporsel:
      type: object
      required:
        - ident
      properties:
        ident:
          description: Fødselsnummer eller D-nummer til en person. Må være 11 siffer.
          type: string
          pattern: "^[0-9]{11}$"

    HttpProblem:
      type: object
      required:
        - type
        - title
        - status
      properties:
        type:
          type: string
          format: uri
          description: A URI reference [RFC3986] that identifies the problem type. This specification encourages that, when dereferenced, it provide human-readable documentation for the problem type
        title:
          type: string
          description: A short, human-readable summary of the problem type. It SHOULD NOT change from occurrence to occurrence of the problem, except for purposes of localization
        status:
          type: integer
          description: The HTTP status code ([RFC7231], Section 6) generated by the origin server for this occurrence of the problem
        detail:
          type: string
          description: A human-readable explanation specific to this occurrence of the problem
        instance:
          type: string
          format: uri
          description: A URI reference that identifies the specific occurrence of the problem. It may or may not yield further information if dereferenced
      additionalProperties: true

  securitySchemes:
    azureAd:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        Bruker [AzureAD](https://docs.nais.io/security/auth/azure-ad) til autentisering. 
        Token må ha `audience` satt til `[cluster].teamdagpenger.dp-stpeter`.
        
        For å teste APIet kan du lage et gyldig OBO-token ved å bruke wonderwalled:
        - [dev-fss](https://azure-token-generator.intern.dev.nav.no/api/obo?aud=dev-gcp.teamdagpenger.dp-stpeter)
